# Additional public opponents: bounded source and contract review

On September 9, 2026, we identified and qualified two untouched public implementations outside V10's current six-opponent gate: **`doomstack_rusher` in its default mode** and a later **Juraj `final-superbot` source archive**. Each passed 480 synthetic public-observation responses across all sixteen 18–21 rectangular shapes, with zero deadline faults, malformed replies or physically invalid actions. No strategy games were played, and no strength or official ranking is established. The [machine evidence](v10-opponent-coverage.json) retains source pins, archive hashes, per-shape measurements and raw-artifact bindings.

## Exact implementations and source review

**Doomstack rusher** is pinned to [Klincent commit `2260b6f19d51a14d7c68770677f22d04dfd88022`](https://github.com/Klincent/generals-bots/tree/2260b6f19d51a14d7c68770677f22d04dfd88022/competition/agents/doomstack_rusher). Its original `agent.py` SHA256 is `4a3744b7dade613e8c894ca9d8b2e5b3f63323e164fcfa565763140200130648`. All three source files were downloaded and checked against their Git blob identities; the repository MIT license is retained separately.

This is separate Python code from Juraj's C++ agent. It expands, collects owned-route donors into a persistent rally, and deploys a concentrated attacker. It reads `STRESS_MODE`, defaulting to `doomstack`; `earlyrush`, `pressure`, and `flood` are additional modes of the same implementation. We explicitly removed that variable and tested only the original default. These modes were not selected by performance. Future comparisons must preserve the absent variable and bind that setting in provenance.

Complete source inspection found standard-library imports, stdin/stdout/stderr, and the single environment read; no network, subprocess, dynamic evaluation, or explicit runtime filesystem access. The entrypoint reads rectangular dimensions, uses perspective-relative ownership, retains one agent per process, flushes five integers per frame, and exits on EOF. It has no castle construction or explicit general-defense/deathtouch policy and may choose routes without sufficient combat force. These are strategic limitations, not protocol violations.

Its original `run.sh` is mode **100644** and executes `python -u main.py` without changing directories. This already fits our actual stdio runner: it invokes `bash run.sh` and sets cwd to the bot directory. No source or executable-mode repair is needed. The probes used that same launch/cwd convention.

**Juraj final-superbot** is the author-published source ZIP at [commit `704ce13d891af22361cb76b9389da92a36b9903c`](https://github.com/Klincent/generals-bots/blob/704ce13d891af22361cb76b9389da92a36b9903c/submissions/final-superbot.zip). The 18,694-byte archive SHA256 is `5eaaf799328bdf39fb9cd4d2ae2cfb0a795eb0cb73d019122747ecbc1ac8a9ca`. It contains only `main.cpp`, `core.hpp`, `build.sh`, and `run.sh`. Original script modes are 0755. The C++17 build writes a local `agent` binary; the launch script changes to its own directory and executes that binary. Source bytes remained unchanged.

We read all four files. The code uses the C++ standard library and stdio; no network, environment-controlled policy options, dynamic execution, or destructive filesystem operations were found. Its clock calls collect timing diagnostics, not action-selection deadlines. Compared with our pinned J35, the main grows from 30,713 to 49,686 bytes and includes additional rear-army collection, muster, doomguard, live castle-cost validation, and anti-cycle behavior. Live castle pricing uses 14. It remains a related Juraj-family implementation, not a new official entrant identity. Static initial passability, approximate route/force reasoning and ordinary-force terminal capture tests remain limitations; source inspection does not certify memory safety or strategy correctness.

## Measured contract qualification

We ran the original entrypoints in fresh processes on CPU 6, one core per bot, with the isolated Python 3.12.10 runtime. Every shape received 30 synthetic public snapshots spanning early, ordinary-combat and deathtouch turns. Terrain stayed fixed; ownership/armies changed synthetically and actions were **not** applied to an engine. These are individually valid observation probes, not complete legal gameplay trajectories. No GPU or opponent match was used.

| Implementation | Responses | First reply range | Later median / p95 / max | Sampled peak RSS |
|---|---:|---:|---:|---:|
| Doomstack default | 480 | 37.20–43.39 ms | 0.558 / 0.644 / 0.800 ms | 16,318,464 bytes |
| Juraj final-superbot | 480 | 18.12–27.93 ms | 0.588 / 0.737 / 0.975 ms | 5,111,808 bytes |

All 32 processes exited normally on EOF. Raw replies, public inputs and stderr were retained. The harness enforced the official 10-second first-response and 150-ms later-response deadlines, checked action shape/source/destination and current castle affordability, imposed a 2-GiB address-space limit, and sampled RSS. Address-space limiting is not the official container memory mechanism. Shared-host measurements and sparse RSS samples do not establish worst-case full-game timing or memory use. [Official interface and environment](https://www.generals.bot/docs).

The original C++ build succeeded in **7.339 seconds**, producing binary SHA256 `b3042fa1b0cf827351bf2ffedd3b84300f5c8e29d3b993145ee74b3030719b5c`. It used host **g++ 13.2.0**, whereas the documented sandbox compiler is **12.2**. Python matches the documented 3.12.10 version. No unsupported C++17 extension was observed, but exact official-image binary compatibility has not been verified. Do not label this an exact compiler-image qualification.

Both candidates are ready for a separately preregistered expansion of local comparison, with full-game validity/runtime checks. Their names and source authors' validation narratives are not measured strength evidence. Pinned J35 remains the known measured gap; this work does not alter V10's frozen campaign or promote a policy.

## Remaining public coverage

The completed family audit adds **ten distinct, unmeasured policies**: eight earlier PR138 variants and two Amin checkpoints. None was removed as an exact duplicate. This is one evolving scripted family and one learned family, with multiple policies in each; family identity does not establish policy equivalence. No opponent module was imported, no inference or benchmark ran, and no new game was played during this audit.

[PR138](https://github.com/strakam/generals-bots/pull/138) remains pinned to `f624c741ad5084be63fc17bacd3961599b8dcc82`. All nine `agent.py` files differ in bytes and in AST after comments/docstrings are removed. `my_bot` and `my_bot2` differ only in `MAX_CASTLES` (3→99) and `LAST_BUILD_TURN` (700→1050) after that normalization; those constants can change actions and do not justify duplicate exclusion. Common main/run files are exact duplicates. Full adjacent-version diffs and prepared source/archive hashes were checked.

| Additional policy | Concrete distinction | Archive SHA256 |
|---|---|---|
| [my_bot](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot) | General-funded castle expander; cap 3, build through turn 699. | `414f26e592418876ee9e11d4dfc08b87778e79efdc3ef673b32925d1dad6f0a8` |
| [my_bot2](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot2) | Same normalized logic, cap 99 and build through turn 1049. | `93350adb91e3ecb4467691aa9f0841f3ccffad853929b46aa9e86769530d7442` |
| [my_bot3](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot3) | Stage and reserve savings from every owned structure. | `d04ffb6da1ab0eb59d2f06c19833cb9047edc9b692c56d23fd7e788c1a071e3f` |
| [my_bot4](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot4) | Remember general, scout and march largest usable stack. | `7e3abf09fc89b509c1c404a353f554b179314ca92ce31f9dd410f57c1770e83a` |
| [my_bot5](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot5) | Add adjacent deathtouch counterchase and attempted stale-cell cleanup. | `e0699b1bceccc71dae6d9e84aecdba306f7031b17cb0500a4f612740ab4c5557` |
| [my_bot6](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot6) | Scout fog behind previously seen enemy territory. | `aef0ca358aa0e6134eec9f3aec6ed7f9e76fc0aa154dbc14908c0e5333599e83` |
| [my_bot7](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot7) | Gather for understrength general attack; stop known-target building after 500. | `4671c42d1c58e55a00eaeff11842a9254da18ca9a33f380654a3c47f22788844` |
| [my_bot8](https://github.com/mrinmoy2developer/gio-competition-tooling/tree/f624c741ad5084be63fc17bacd3961599b8dcc82/competition/agents/my_bot8) | Choose nearest reachable packet for deathtouch race. | `0dd2ea5df89ab868cd948733ede1fd352be88e2b8c586b45d13b8eac0173a78b` |
| [my_bot_main7_iter79](https://raw.githubusercontent.com/Amin-Debabeche/generals-bots/34506fe2d684f163ada3a0cc3401d192436bfc37/competition/agents/bot_archive/my_bot_main7_iter79.zip) | Same CNN/code as main8; different weights. | `8fbe9b646b85002f1cbb211e6678c44b257bafb8cad5e49856c0855552c51088` |
| [my_bot_main7_iter308](https://raw.githubusercontent.com/Amin-Debabeche/generals-bots/34506fe2d684f163ada3a0cc3401d192436bfc37/competition/agents/bot_archive/my_bot_main7_iter308.zip) | Same CNN/code as main8; different weights. | `3978872c0ca01900e7ffc4239cc65a67a607f6d53efb3307ebf73bfb0fdff7e2` |

The eight mybot links identify original source directories. Their archives are locally constructed deterministic bundles of three unchanged files plus the root MIT notice, not author-published ZIPs. The Amin links identify the original author-published archives. The machine evidence retains all local archive paths, original Git blob identities and source URLs. The original mybot proximity-price constant remains 10 versus the current 14; no rule repair was applied, so invalid builds must remain visible in any future comparison.

Both Amin ZIPs were downloaded at [`34506fe2d684f163ada3a0cc3401d192436bfc37`](https://github.com/Amin-Debabeche/generals-bots/tree/34506fe2d684f163ada3a0cc3401d192436bfc37/competition/agents/bot_archive) and matched the pinned tree's size and Git blob SHA1. All **five non-weight members**—agent, main, NumPy inference, run script and metadata—are byte-identical to the tested main8 iter160 archive. All three use the same 21×21 CNN, with 19 input channels, five convolution/ReLU layers, a nine-action cell head and a pooled pass head. There are **14 float32 tensors and 138,010 finite parameters**, with identical names/shapes. Every tensor differs in each checkpoint pair: iter79 versus iter308 changes 131,151 parameter values; versus main8, they change 133,596 and 133,193 respectively. Distinct weights remain unmeasured policies; tensor inequality is not a measured action difference or strength ranking. None of these archives uses the separate transformer helper.

ZIP paths/sizes and NPY headers were checked before data loading; symlinks, unsafe paths, duplicate members and object arrays were rejected, with `allow_pickle=False`. The identical Amin code uses only standard-library/NumPy dependencies and local weights/metadata reads. Its build constants match 35/14/2. Its inherited 21-canvas move mask can nevertheless select a destination in padding on smaller rectangles: the final guard checks the source against actual dimensions, not the destination. Preserve original invalid-action accounting. Shared code does not transfer main8's measured deadlines, invalid rates or outcomes to either new checkpoint; these ten policies still need separate contract/runtime qualification.

The bounded public-policy subtotal now comprises **18 default policies**: six current gate policies, these ten family policies, doomstack default and later Juraj. The original doomstack `earlyrush`, `pressure`, and `flood` modes add three unmeasured configurations, for **21 named configurations**. Pin each mode before evaluating it; sharing implementation does not exempt it from comparison. This is a public-coverage subtotal, not complete local-control coverage, and does not claim all public bots have been found.

The local JAX Expander samples from normalized positive move scores; the stdio Python/C++/Rust Expanders choose a deterministic argmax. For an unobstructed home with three armies and four equally scored capture options, JAX assigns equal weights whereas the stdio tie order chooses up. This is a source-derived distinction, not a new inference test: wins against JAX Expander cannot be transferred to the stdio implementation. The three stdio ports have matching source algorithms and now have exact action parity on the bounded synthetic corpus below; universal equivalence remains unproved. Harvester equals Hunter only under the no-neutral-castles condition used by these competition starts, where neither bot builds; do not generalize that identity to arbitrary maps. Deterministic stdio Expander, local Random, and our learned/legacy controls still need their own comparison gates.

## Stdio Expander port qualification

The untouched C++, Python and Rust ports each completed **480/480 responses across all sixteen 18–21 rectangular shapes**, with zero deadline faults, malformed replies or physically invalid actions. On every shared observation, all three emitted exactly the same raw and validity-normalized command: 480 three-way matches, or 1,440 pairwise comparisons. Each port’s sixteen fresh processes exited normally on EOF. The [machine evidence](v10-opponent-coverage.json) embeds the report and source/runtime bindings; the cache report SHA256 is `e452692c7c4e7fcea277611ce3409e9121af62c5b42121999a624a6dfdef1898`.

| Port | First reply range | Later median / p95 / max | Sampled peak RSS |
|---|---:|---:|---:|---:|
| C++ | 8.63–10.05 ms | 0.140 / 0.184 / 0.251 ms | 3,342,336 bytes |
| Python | 42.81–100.37 ms | 0.554 / 1.983 / 3.300 ms | 15,335,424 bytes |
| Rust | 8.19–13.43 ms | 0.068 / 0.111 / 0.178 ms | 1,769,472 bytes |

Only git-tracked source files were copied from commit `c8d84456a1b0404f5b34863bc57289b5b5ddcbe3`, with original file modes and the repository MIT notice. Existing repository binaries, Rust target caches and Python bytecode were excluded. Every source byte was checked against its Git blob and SHA256 before and after probing. The complete policies, entrypoints and build scripts use standard libraries and local compilation; no network-dependent code or policy modes were found. The original Rust manifest has no dependencies. Original C++ and Rust builds succeeded offline in 0.776 and 1.231 seconds respectively; no package installation or source repair occurred. Binary hashes and complete source manifests are retained in the machine evidence.

All builds/probes used CPU 6 only. Python was 3.12.10, g++ 13.2.0 and Rust/cargo 1.97.1. The available C++ compiler differs from the documented 12.2; Rust used the host toolchain. This is not an exact official-image qualification. Each original `run.sh` was invoked through bash from its required bot directory, with a 10-second startup-inclusive first deadline, 150-ms subsequent deadline and a 2-GiB address-space limit. Timing is from a shared host; RSS sampling is not continuous or equivalent to the official container memory mechanism.

The same thirty individually valid public snapshots per shape had fixed terrain and synthetic ownership/armies. Actions were **not applied to an engine**, so “applied parity” here means only validity-normalized commands; every observed command was valid and timely. This establishes bounded contract/parity evidence, not action-consistent gameplay, universal port equivalence, worst-case timing or strategic strength. These ports issue full moves/pass and do not build or split. No optional JAX inference ran. Their deterministic selection remains distinct from JAX Expander, so current JAX matchup results do not establish stdio Expander strength.

## Search boundaries

This was a bounded search of seven current default trees and two additional pinned Juraj branches, plus the first 100 Juraj branch records. Four sampled forks had no extra named competition agent/model artifact beyond starter baselines. Current Klincent master lacks runnable Juraj source, but historical commits and other branches remain public. The search is not exhaustive across every fork or historical variant.

The [leaderboard](https://www.generals.bot/leaderboard) describes preliminary qualification standings; the [results page](https://www.generals.bot/results) describes a separate archived tournament. Neither retrieved page provided runnable entrant downloads. The browser tool could not open the leaderboard data API, so no fresh numeric ranking is claimed. We did not access authenticated/private artifacts or establish official identity-to-source mappings. Unavailable entrants remain untested; beating these public references would not prove that all official bots are beaten.
